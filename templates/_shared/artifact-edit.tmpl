**Artifact (required)**
- Read `{source}` before writing.
- Update sections {sections} in `{output_path}` on disk — not chat-only. Create `{artifact_dir}/` if missing.
- Leave other sections unchanged.
- Use schema headings exactly; substitute real work id (`001`) when known.
