---
description: 'Command with branch-only next step (no default command)'

---

Skills: @skill-scoped

Branch-only handoff body.

**When done — print this as your final message:**

### Next recommended step
Inspect project state and pick **one**:
- **more work remains** → `/cmd-next-branches` with @skill-scoped, or @skill-plain (Skills are NOT auto-attached)
- **all branch tasks done** → `/cmd-minimal`