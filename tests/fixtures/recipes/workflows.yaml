commands:
  cmd-minimal:
    review: verify sample output
    next: cmd-skills-direct

  cmd-skills-direct:

  cmd-skills-skillset:
    next: cmd-minimal

  cmd-next-rich:
    review: Verify alpha section content
    review_section: Alpha
    next: cmd-minimal

  cmd-next-branches:
    next: cmd-minimal
